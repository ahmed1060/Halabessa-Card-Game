{
    "rules": {
        "matches": {
            "$matchId": {
                ".read": "auth != null",
                ".write": "auth != null && (!data.exists() || data.child('players').hasChild(auth.uid))"
            }
        },
        "matchmaking_queue": {
            ".read": "auth != null",
            "$uid": {
                ".write": "auth != null && auth.uid == $uid"
            }
        },
        "users": {
            ".read": "auth != null",
            "$uid": {
                ".write": "auth != null && auth.uid == $uid"
            }
        }
    }
}